# Copy to apps/web/.env.local for local development (values from Supabase → Settings → API).
# The app reads these at build time (see apps/web/src/lib/env.ts and apps/web/src/config.ts).
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY
